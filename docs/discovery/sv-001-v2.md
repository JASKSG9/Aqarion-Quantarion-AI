# SV-001-V2 — Discovery Record

**Status:** DECLARED_PENDING_REPLAY · C4 BLOCKED

## Purpose

Record the correction of the historical SV-001 domain and the architecture
of the executable replay contract.

## Critical correction

The historical governance layer used **5720 cases**. The canonical finite
domain is instead

    m ∈ {2,…,8},  k ∈ {2,…,8},  s ∈ {1,…, mk−1}

giving

    1176 total cases
     196 with s mod k = 0
     980 with s mod k ≠ 0

The value 5720 is **deprecated** and must not appear as an active execution
count in any verifier or CI step.

## Contract

Let r = s mod k, α² = r(k − r) / k², Q = UUᵀ, D = (I − Q) K Q.

    Uᵀ Dᵀ D U  =  α² (2I − S − Sᵀ)
    tr(Uᵀ Dᵀ D U)  =  2 m α²
    ‖D‖₂  =  2 √(α²)                (m even)
    ‖D‖₂  =  2 √(α²) cos(π / 2m)    (m odd)

## Replay architecture

    contract  →  oracle  →  verifier  →  mutation  →  metamorphic
              →  replay  →  receipt  →  hash binding  →  CI

## Evidence boundary

A passing floating-point replay is computational evidence. It is not:

- a Lean proof;
- an exact rational proof;
- a publication certificate;
- evidence for unrelated AQARION claims;
- an independent external reproduction;
- a C4 promotion.

## Observed development-run results

Prior local development execution reported:

- 1176 / 1176 cases
- Gram failures: 0
- Trace failures: 0
- Norm failures: 0
- Mutation suite: 5 / 5 killed
- Metamorphic suite: PASS

These are development results. They become repository replay evidence only
after the files are committed and GitHub Actions executes
`.github/workflows/sv-001-v2.yaml`.

## Governance

C4 remains BLOCKED. No promotion occurs merely because this contract passes.
