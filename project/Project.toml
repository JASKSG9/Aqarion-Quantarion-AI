[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aqarion-quantarion-ai"
version = "0.1.0"
description = "Finite dynamics, AI-assisted discovery, and claim evidence governance"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
